package nine.sample.webview

class C {
    object Push {
        const val FCM_KEY = "fcmkey"
    }

    object Web {
        const val APP_BRIDGE_NAME = "AndroidBridge"

        const val TARGET_URL = "targetUrl"

        const val METHOD_KEY = "methodKey"
        const val METHOD_GO_URL = "targetUrl"
        const val METHOD_AUTO_LOGIN = "autoLogin"
        const val METHOD_LOGOUT = "logout"
    }
}